const add      = require('./demo_add')
const yesno    = require('./api_yesno')
const u_agent  = require('./api_u_agent')
const forecast = require('./api_forecast')
module.exports = {
  add,
  yesno,
  u_agent,
  forecast,
}
