baseurl: 'https://' # comments
msg: Hi

env:
  dev:
    greet: '{msg} from DEV'
  qa: 
    greet: '{msg} from QA'
  noreplace: 'no change on var'

default:
  headers:
    Content-Type: application/json

greet: Hi from non ENV
greet-ed: 'hello {{dtnow}}'
